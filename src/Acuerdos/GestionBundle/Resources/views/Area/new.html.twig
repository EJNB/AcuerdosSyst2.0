{% extends '::layout-admin.html.twig' %}

{% block content %}

    <h3 class="page-header">Crear Area</h3>

    <div class="col-md-3">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-actions">
            <button type="submit" class="btn btn-primary">Aceptar</button>
            <a class="btn btn-default" href="{{ path('area') }}">Regresar al listado</a>
        </div>
    </div>

    {{ form_end(form) }}

{% endblock %}
